let obj = {
    x:1, y:2, z:3
}

let keys = Object.keys(obj) // returns array of keys 
console.log(keys);

let value = Object.values(obj) // returns array of values
console.log(value);


let entries = Object.entries(obj) // returns arraay of key value pair of object

console.log(entries);
